<% define_content :link do %>
  <%= HostUrl.protocol %>://<%= HostUrl.context_host(asset.course) %>/<%= asset.course.class.to_s.downcase.pluralize %>/<%= asset.course_id %>/details
<% end %>

<%=
    case asset.type
    when 'TeacherEnrollment'
      t :body_teacher, "%{user} just accepted their invitation to participate in the project %{course} as a hiring manager.", :user => %{<b><a href="#{content :link}">#{asset.user.name}</a></b>}, :course => asset.course.name
    when 'TaEnrollment'
      t :body_ta, "%{user} just accepted their invitation to participate in the project %{course} as a interviewer.", :user => %{<b><a href="#{content :link}">#{asset.user.name}</a></b>}, :course => asset.course.name
    when 'ObserverEnrollment'
      t :body_observer, "%{user} just accepted their invitation to participate in the project %{course} as an HR.", :user => %{<b><a href="#{content :link}">#{asset.user.name}</a></b>}, :course => asset.course.name
    when 'DesignerEnrollment'
      t :body_designer, "%{user} just accepted their invitation to participate in the project %{course} as a designer.", :user => %{<b><a href="#{content :link}">#{asset.user.name}</a></b>}, :course => asset.course.name
    else
      t :body_student, "%{user} just accepted their invitation to participate in the project %{course} as a candidate.", :user => %{<b><a href="#{content :link}">#{asset.user.name}</a></b>}, :course => asset.course.name
    end
%>
